$kind: http-request
description: Returns the authenticated user's profile. Requires a valid JWT
  token in the Authorization header. Run Login User first to set the token
  variable.
url: "{{baseUrl}}/api/profile"
method: GET
headers:
  Authorization: Bearer {{token}}
examples: ./.resources/Get Profile.resources/examples
order: 3000
