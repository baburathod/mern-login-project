$kind: http-example
request:
  url: "{{baseUrl}}/api/profile"
  method: GET
  headers:
    Authorization: Bearer {{token}}
response:
  statusCode: 200
  statusText: OK
  body:
    type: json
    content: |-
      {
        "name": "John Doe",
        "email": "john@example.com"
      }
order: 1000
